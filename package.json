{
  "name": "cloudtype-github-action-deploy",
  "version": "0.0.0",
  "description": "Cloudtype Github Actions - Deploy",
  "main": "src/action.js",
  "scripts": {
    "build": "ncc build src/action.js --license LICENSE",
    "dev": "npm run build && act workflow_dispatch -s GITHUB_TOKEN=$GHP_TOKEN",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "joje@cloudtype.co.kr",
  "license": "Apache-2.0",
  "devDependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "@vercel/ncc": "^0.32.0"
  }
}
