{
  "name": "cloudtype-github-actions-deploy",
  "version": "0.0.0",
  "description": "Cloudtype Github Actions - Deploy",
  "main": "dist/index.js",
  "author": "Cloudtype Inc",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloudtype-github-actions/deploy"
  },
  "keywords": [
    "cloudtype"
  ],
  "scripts": {
    "build": "ncc build src/action.ts --license LICENSE",
    "dev": "npm run build && act workflow_dispatch -s CLOUDTYPE_TOKEN=$CLOUDTYPE_TOKEN -s GITHUB_TOKEN=$GHP_TOKEN",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write **/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "@types/node": "^16.11.12",
    "@vercel/ncc": "^0.32.0",
    "prettier": "^2.5.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "json5": "^2.2.0",
    "node-fetch": "^3.1.0",
    "yaml": "^1.10.2"
  }
}
